# Optional: local voice clone on CPU / AMD (no NVIDIA)
# py -3 -m pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
# py -3 -m pip install chatterbox-tts --no-deps
chatterbox-tts>=0.1.0
