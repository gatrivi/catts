FROM python:3.12-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .

ENV CATTS_DATA_DIR=/data
ENV CATTS_API_HOST=0.0.0.0
ENV CATTS_API_PORT=59200

EXPOSE 59200
VOLUME ["/data"]
CMD ["python", "-m", "api.main"]
