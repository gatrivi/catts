# AGENTS.md (repo root — Cloud Agents)

1. Read `docs/SESSION_CONTEXT.md` first (token-cheap truth).
2. Cloud/web runs: `docs/CLOUD_AGENTS.md` — this PC is **My Machines** worker `catboxprime`.
3. TTS/API: `http://127.0.0.1:59200` · topic docs one-at-a-time (`EXTERNAL_TTS.md`, `AUDIO_ENGINE.md`, `HEAVY_PROCESSES.md`).
4. Python: `.\.venv\Scripts\python.exe` only. Kill heavy workers after batch.
5. Voice clone: train-once preference — `docs/VOICE_CLONE_PREFERENCE.md`. Don't retune XTTS.
6. Fish audiobooks: **one locked ref** or voices randomize per chunk — `docs/FISH_VOICE_LOCK.md`.
7. No PR unless asked. Warn before ~500k tokens.
