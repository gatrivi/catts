# HANDOFF — GPU/kernel track + campaign (2026-09-25 evening)

Single entry point for any fresh session (human or agent). Read this, then
`WORKFLOW.md` (the operating ladder), then `GPU_TRACK_FINDINGS_2026-09-24.md`
(full track history). Do not re-derive anything below — artifacts are on disk.

## 1. Where the track stands (measured, verified)

| Model | Decode | Change | Proof |
|---|---|---|---|
| TQ2_0 (daily driver) | 12–13 → **20.5 t/s server decode** (13.0 wall w/ logger) | **GPU-sum 81 → 53 ms/tok (−35%)** | per-op captures + GGML_VK_DISABLE_MMVQ A/B (causality direct) |
| PTQ1_0 (fidelity path, golden 7/8) | 6.4 → **7.4–7.9 t/s** | GPU-sum 157.8 → 129.2 ms/tok (−18%) | suite 68/68 + golden 7/8 (`data/golden/confirm-20260924-195613.json`) |

- Physics: TQ2_0 floor = 29 ms/tok at 100% BW (224 GB/s ÷ 6.48 GB). We are at 53 ms
  = 55% of peak. Card's real copy rate: **185 GB/s** (64–256 MB); 445–644 GB/s when
  Infinity-Cache-resident (≤16 MB). lm_head streams at 193 GB/s cold (q4_0-class).
- **Roofline verdict (measured 2026-09-25, `docs/KERNEL_HEADROOM_2026-09-25.md`)**:
  36.6 ms of the 53.4 ms step (68%) is tq2_0 matvecs running at **156-202 GB/s
  against the 185 GB/s measured copy rate = 90-100% of roofline**. lm_head is only
  2.9% of the step and is the fastest op per byte. The matvec kernel is DONE;
  remaining recoverable kernel time is ~4.3 ms (GET_ROWS 1.6, rms_norm 1.6, MoE
  per-expert batching 0.5, MUL 0.6). The 2× lives in speculative decode.
  vecq A/B (DISABLE_MMVQ=1) confirms it wins on all 7 production shapes
  (1.16-1.80x) — no silent fallback anywhere.
- Per-dispatch floor = **2.29 µs** (ADD 16 KB); 724 dispatches/token = 1.7 ms
  irreducible. Use it as the yardstick for every small-op complaint.
- User targets: 20 ms/t *effective* (needs speculative decode working + near-peak
  kernel) and 50 t/s aspiration (on a 3060's 360 GB/s the author measures 50.1 —
  on our 224 GB/s the equivalent is ~29–31 t/s; state this in public).

## 2. Running right now
- **The opencode PTQ1_0 agent is DEAD, not working** (verified 21:00 on 09-25):
  `opencode-run.log` last write 16:48, no `ITERATION_LOG.md`, zero file writes in
  the tree for 90+ min, tree clean at `e728b26` + the `qwen35.cpp` #217 fix only.
  This tripped the handoff §7.2 stall rule, so the tree is FREE — the mtp-lean
  #217 test is unblocked. **One writer per tree still applies** (to me).
- **Night 2 fires 02:30** (`CATTS-NightJobs` → `night_campaign.ps1`). Day 2 slices
  are now: **MTPLean, RGP, Bandwidth, TypeMatrix-q4_0** (RGP + MTPLean added
  tonight). MTPLean is RAM-guarded (skips below 11 GB free) and records
  `mtp:lean-draft-mtp` + `mtp:acceptance_speedup` — first ever run of the #217 fix.
- `CATTS-GPUConfirm` is **Disabled** (correct — it cannot see dGPU video decode).
  Re-enable only if you want the daytime poll back.

### Live risks for the 02:30 night (found 22:30, NOT fixed — user chose wrap-up)
1. **`llama-server-evening` fires 23:30 on :8123** with Qwen3.5-9B Q4_K_M
   (~5.3 GB VRAM, window 23:30–07:30 by owner rule) and `llama-server-morning-stop`
   does not kill it until **07:30**. The campaign waits up to 90 min for a clear
   GPU. Night 09-25 ran fine, so "wait then continue" is the normal behaviour —
   but if the 9B is up, expect the first slices to slip ~30–90 min.
   One-line mitigations if it bites: (a) `schtasks /Run /TN llama-server-morning-stop`
   before 02:30, or (b) shrink the wait loop.
2. **A blocked night is never retried.** `night_campaign.ps1` writes `lock.flag` at
   start; if it exits as `night:blocked` the flag stays and the next day's run
   exits immediately. Fix = delete the lock on the blocked exit path.
3. **No VRAM guard.** `test-backend-ops` lm_head (303 MB) + RGP traces are fine
   alone, but a resident 9B server leaves <3 GB VRAM; large shapes would fail
   late and silently.
4. **Ports**: RDS on UDP 27300 (harness reuses or starts it); the RGP harness
   reuses an existing RDS and skips if a foreign process holds the port.
   ServerProbe uses 9103 — the GPU-wait loop already treats 9103/9151 as busy.

## 3. Armed automations (zero cloud tokens)
- `night_campaign.ps1` (23-night schedule, day = nights since 2026-09-25):
  bandwidth ground truth, per-type matrices, config axes (GGML_VK_DMMV_REDUC,
  GGML_PTQ1_0_WG), spec-decode A/Bs, long-context, workload characterization,
  gap-fill, RGP capture, knowledge-pack compile + premortem (nights 21–23).
- Output: `data/campaign/<date>/` + `knowledge.jsonl` (25 cells so far) +
  `coverage.json`. Stop-guard **07:00–20:00** (was ">07:00", which wrongly blocked
  evening manual runs). Never kills processes. Per-slice hardening NOT yet done:
  no try/catch around slices, no VRAM/RAM guard on server probes.

## 4. Artifacts on disk
- **Downloads (SHA-verified)**: `data/models/bonsai2-27b-ptq10-mtp/Ternary-Bonsai-2-27B-PTQ1_0-mtp-lean.gguf`
  (6.30 GB, 1e33c571…), `data/models/dspark-drafter/Bonsai-27B-dspark-Q4_1.gguf`
  (1.79 GB, 25e73f9f…).
- **Profiling toolchain**: `C:/tools/rdts/RadeonDeveloperToolSuite-2026-05-28-1806/`
  (RGP + PanelCLI + service + spirv-as/dis + amdgpu-dis). Source zip also in
  `Z:/vaultz/`. RGP service (RDS) listens on **UDP 27300**; the CLI only works
  with `--remote-host 127.0.0.1`.
- **RGP captures**: `data/profile/rgp/` — 4 traces + extracted shader `.elf`s
  (tq2_0 vs q4_0, tiny + lm_head), `last_<tag>.json` per job, ISA byte counts in
  `data/campaign/knowledge.jsonl` (`isa:code_bytes-*`, `rgp:*`).
- **NOT in supermemory**: the RGP lessons (remote-host flag, dispatch 3, `type_a=`)
  could not be ingested — `memory_ctx.py --add` needs an already-running model slot
  and none was up (2026-09-25 22:40, deliberately not started: 4.4 GB RAM free
  while the owner works). The durable record is this handoff +
  `RGP_ATOM_CAPTURE_2026-09-25.md` §5. Re-run `--add` when a slot is up.
- **Provenance**: `data/provenance/build-hashes-20260924.txt`, `suite-log-20260924.txt`.
- **Per-op captures**: `data/profile/d1_perop_2026092*.json` (8K + 32K, both models,
  pre/post port); baselines `d1_20260924-*.json`, `d1_20260925-100004.json`.
- **Shader backups**: `data/shader_baks/mul_mat_vec_ptq1_0.comp.bak-{e2,e2a}`.
- **Kernel tree**: `C:/src/llama.cpp` (git repo, baseline commit e728b26 + working
  edits: qwen35.cpp #217 Hadamard fix + opencode.json + agent's shader WIP).

## 5. Verified results / refuted (do not re-test blindly)
Full list in `GPU_TRACK_FINDINGS_2026-09-24.md` §4–5. Top items:
- int-dot vecq port: WIN on TQ2_0 (−35% production). **In-suite ties are not
  production ties** (IC-warm masking) — production perop is the only speed oracle.
- Refuted with measurements: 5 matmul designs; load redundancy (V-C regressed);
  LDS size beyond int8; workgroup width (64 tie / 128 breaks a batched case);
  element-major repack (premise refuted); ngram on prose (marginal); DSpark on
  TQ2_0 (cross-gen spec-batch error); mtp-lean on b10709 (Hadamard-latent format —
  needs the author's build or the #217-style port, which is now applied in-tree
  but UNTESTED because the agent's shader breaks builds mid-flight).
- Zero-orphan correction: Z:/ollama blobs are REFERENCED working models. Nothing
  deleted. Real delete candidates: `data/campaign/model_inventory_20260925.md`.

## 6. Pending decisions (user)
1. **Delete list** (~20 GB: coder7 ×2, nanbeige, qwen35-4b, gemma4-coder, granite —
   `model_inventory_20260925.md`) → frees room + closes the inventory. Two models
   (gemma4-coder, granite) have zero eval records: eval-before-delete is an option.
2. **3060 46 t/s source** (anchors the public thread; first reply will ask).
3. **Post the tweet thread** (`docs/TWEET_THREAD_2026-09-25.md`; add fork links).
4. **MTP-lean test — NOW ARMED, runs tonight.** Tree has been stable since 16:48
   (that agent stalled out; tree clean at `e728b26` + #217 fix, no ITERATION_LOG).
   `MTPLean` slice in `night_campaign.ps1` runs mtp-lean + dspark drafter with
   `--spec-type draft-mtp`, RAM-guarded (11 GB free, so it skips if you're using
   the machine), records `mtp:lean-draft-mtp` + `mtp:acceptance_speedup`. This is
   the first execution of the #217 fix and the only untested spec path — the 2×.
5. **Upstream PR** (optional): int8 LUT + vecq repair + #217 backport + numbers.

## 7. Next actions (priority order, respecting the ladder)
1. Read tonight's campaign output (`data/campaign/20260926/`) at dawn.
2. When the opencode run finishes or stalls >4 h without ITERATION_LOG growth:
   read its log, take the working state or revert to bak-e2a, then test mtp-lean
   with draft-mtp (Phase B spec in `D2_ROUND2_RUNBOOK_2026-09-24.md` tail).
3. **RGP atom-level capture: SOLVED + automated** (was the §7.3 blocker).
   `scripts/rgp_capture.ps1` (guarded, headless) + `scripts/rgp_isa_notes.py`
   (headless ISA reader); `night_campaign.ps1` RGP slice is no longer a stub — 4
   captures in ~2 min. Two non-obvious fixes: the CLI needs
   `--remote-host 127.0.0.1` or the capture API never initializes, and
   dispatches 1-2 are transfers (RGP `-2`) — the matmul is dispatch 3.
   Findings + how to read the traces: `RGP_ATOM_CAPTURE_2026-09-25.md`.
   **The TQ2_0 kernel is 1.71x the ISA code size of q4_0** (11788 vs 6872
   bytes) — the vecq −35% was bought with ~4.9 KB more instructions. Still
   needs one 10-min GUI read for occupancy/stall counters.
4. Close the track with the verdict: PR (optional), roster final, findings final.

## 8. Gotchas (each one cost real time — respect them)
- llama-server (C:/src build) needs `C:/tools/llvm-mingw/bin` in PATH or it dies
  silently (0-byte log). Launch scripts must set it.
- Shader changes: touch `mul_mat_vecq.comp` to force regen; relink llama-server
  TOO (not just test-backend-ops); header only rewrites in FULL gen mode
  (`vulkan-shaders-gen --output-dir ... --target-hpp ...` with no --source).
- One writer per tree. opencode's project root = the git root — `git init` fixed
  the external_directory rejections.
- test mode culls large shapes → production correctness = golden gate only.
- Sub-32 MB suite numbers are IC-inflated → lm_head 248320×5120 is the A/B shape.
- PowerShell 5.1: no utf8-sig; `Test-Path $x -and y` parses wrong; backslash
  escapes in heredoc-written strings corrupt PATH entries (use raw strings).
  `[IO.File]::WriteAllText` with `UTF8Encoding($false)` to strip a BOM after any
  `Set-Content -Encoding UTF8` rewrite of a .ps1.
- test-backend-ops perf-mode op filter uses `type_a=`, **not** `type=` — a wrong
  name matches zero cases and the process exits instantly (looks like a capture
  failure, not a filter bug).
- LLm rule: `--reasoning-effort medium` on Bonsai serving (xhigh burns the budget);
  json_object truncates.
